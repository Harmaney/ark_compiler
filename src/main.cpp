#include "lex.h"
#include "parser.h"
#include "test_ast.h"
using namespace std;

int main(int argc, char **argv) {
    // lex_work("1.pas");
    // parser_work("lex_out.txt");
    RUN_TEST();

    return 0;
}